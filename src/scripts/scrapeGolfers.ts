// import { getCourseCollection } from "../models/course-model";
// import pubsub, { JOBS } from "../pubsub/pubsub";
// import logger from "../util/logger";

const scrapeGolfers = async () => {
  // logger.info('starting to scrape golfers')
  // const collection = await getCourseCollection();
  // const cursor = await collection.find({})
  // while (await cursor.hasNext()) {
  //   const course = await cursor.next();
  //   if (!course) continue;
    // const agenda = await pubsub.getPubSub();
    // await agenda.now(JOBS.processGolfers, { course })
  // }
  // logger.info('done scrapping golfers')
}

export default scrapeGolfers;
  